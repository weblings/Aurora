# Windows headless dual-mode (LogSink, attach, rewire, flip; live pass paused)

`Aurora-7l1` specified as dual-mode, then split into sequenced children `7l1.1`–`7l1.7` with dependencies (logger → attach/second-instance/fatal → flip → verify; session-end window parallel). Bead carries the sink table (design: all 17 `cout`/`cerr` sites dispositioned) and four settled opens (notes: two-phase log init with `%TEMP%` fallback, only-Fatal boxes, per-mode stop line + OSC-8 strip in file sink, hidden top-level window for session-end since `HWND_MESSAGE` never receives those broadcasts — docs-settled).

Implemented on `feat/WindowsNoTerminal` (`9fcfe2f`–`85c53ee`): `LogSink` in `AuroraApp` (buffer-from-start, replay to `<configRoot>/aurora.log` in order, raw-to-console/stripped-to-file tee, flush every line per the windows-env buffering lesson, `wantsConsole` for `--console`/`AURORA_CONSOLE`, `filePath`); `attachParentConsole` + `CONOUT$` reopen (attach iff parent exists, `AllocConsole` when forced); full `main.cpp` rewire (only sink-internal `cout` refs remain); silent-unless-attached second-instance handoff; `Fatal` box naming the log path (only boxing site); hidden `AuroraShutdown` window sharing `trayWndProc`; `WIN32_EXECUTABLE TRUE` with plain `int main` kept (no WinMain shim, argv parsing unchanged).

Verification split by environment: portable logic probe-green via a `/tmp` assert-driver mirroring the committed Catch2 cases (kept out of the repo); the Catch2 suites could not execute here (no Catch2/nlohmann/httplib, no network for FetchContent) and are `windows.yml`-gated; all Win32/`main.cpp` hunks re-read only, MSVC-pending. Surprises: the probe caught a real `setFile` re-entry bug (`is_open` on an already-open stream masks a failed reopen — fixed open-then-move, regression covered by the `filePath` case) and one artifact of its own (stale temp dir + append mode — temp isolation per run); both filed as `docs/lessons/debugging-method.md` and `windows-env.md` entries, guard passes.

Paused with code landed, `7l1.1`–`7l1.6` in `in_progress`, `7l1.7` open: the six-check live pass needs a Windows machine (MSVC Release build; double-click no-flash + tray; cmd print + Ctrl+C; `--console` from Run; silent second-instance handoff; headless fatal log + box; logoff/shutdown clean exit). Close each sub-bead as its check goes green, then close the parent.

# gj0.5 refactor (3 slices) + gj0.6 viz page, both browser-verified on Linux

Closed `Aurora-gj0.5`/`gj0.6` (`1.0.3`/`WebFakeLights`). The Mac session had
deliberately left gj0.5 unstarted (no browser there); the reporter's "no
regressions" check covered the demo as-is, not the described split -- bead
reopened, then landed stepwise with Firefox verification per slice:
`scene-core.js` (`ccfe891`), point/rectArea/room rig modules (`2141780`),
video/audio source modules + provider contract (`b88d69a`). main.js 935→460
lines; rectArea carried unchanged. gj0.6 (`1e6eefd`): `viz.html` + `viz.js` +
`LiveDataSource` on the shared core, frame-apply shared with main.js, node
mapping tests green. Live proof: split + single-zone relay frames drive the
right lamps (id 2 → back-left couch lamp); dark-until-first-frame added after
the as-loaded room read all-white. Demo footer bumped to 1.0.3 (CHANGELOG +
shim `/api/version`, pinned by test -- also fixed that suite's pre-existing
version-assert failure). Native version untouched.

Surprises: post-pull `bd import` cycle silently reopened 6 Mac-closed beads
(stale-DB auto-export clobbered the pulled file; caught via `git diff`,
fixed via checkout + re-import) -- lesson filed in `architecture-process.md`.
A "red, blue, green, white" split report nearly triggered a mapping edit
before the reporter clarified the order wasn't positional -- single-channel
probing lesson filed in `debugging-method.md`. Mac-filed `Aurora-y1q`
(audio-ifdef follow-up) never reached the export; re-filed as `Aurora-4g2`.

State: gj0.5/.6 closed. Next is gj0.7 (full Linux validation: real capture
→ fake bridge → relay → viz); needs an app/linux build, cmake missing here.
`Aurora-1t1` (PipeWire fullscreen freeze) and `8mk.7` Linux check still open.

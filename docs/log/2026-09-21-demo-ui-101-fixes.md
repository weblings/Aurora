# Demo UI 1.0.1 fixes (2026-09-21)

Aurora-egp (top tier visible in demo) and Aurora-5jj (slider thumb off-center in WebKit): both fixed, both verified on deployed gh-pages and the local Windows app, both closed.

## Fixes (d2911c2, on dev)

- egp: `.db-port .db-top-tier { display: none; }` in demo-layout.css (demo-owned override, #db-change-bridge pattern); redundant inert hook + dim rule removed. Logo + Stop live in the top-bar slot, unaffected. topTierError loses its demo home; shim always succeeds so unreachable.
- 5jj: track/thumb sizes as --slider-track-height/--slider-thumb-size vars on .slider-input, `margin-top: calc((track-thumb)/2)` on ::-webkit-slider-thumb only (Gecko auto-centers; untouched). Applied verbatim to web/ui and the vendor fork. Root cause: iOS Firefox is WKWebView -- desktop-Firefox verification had tested the wrong engine.
- Tests: new web/ui/styles/forms.test.mjs (relationship pin, both copies), demo-layout.test.mjs extended. Authored but not run here (no node in this env); real check was the gh-pages render.

## Process findings

- Live beads DB was a day behind the pulled export with no dolt remote to pull from; recovered with `bd import` (upsert). Workflow recorded in AGENTS.md: sync rides git, never `--reinit-local`.
- gh-pages branch (subtree root) is the live deploy, not the demo-pages Actions workflow -- workflow deleted. Subtree push from dev blocked on missing push credentials in this env; user runs `git subtree push --prefix web/demo origin gh-pages`.
- Still open: Aurora-2uw (dashboard skeleton, same 1.0.1 label).

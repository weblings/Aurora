# --fake-hue flag + gj0.7 agent-run validation (Linux)

`--fake-hue` added to app/linux (`7bf0e75`): presets bridge
address/credentials/`conf-room-4zone`/dev-discovery as env defaults
(explicit env wins, store wins over both). Header-only `FakeHue.hpp` +
Catch2 `FakeHueTests.cpp` (12 assertions, run green against the prebuilt
Catch2). Naming per repo convention (hyphenated multi-word flags).

Full stack launched detached by the agent (bridge → relay → app
`--fake-hue --fresh` + tap → zone map → headless API pairing) and
validated to the SSE endpoint; reporter opened viz.html and confirmed
lamps track the real screen -- gj0.7's trust step, independently
re-proven. Recipe + agent-handoff note ("hand the user the viz URL")
written to `tools/light-viz-relay/README.md`; `--fake-hue` pointers in
both tool docs; viz entry in `web/demo/AGENTS.md`.

Surprises: the first relink deleted `build/linux-app/bin/Aurora` (stale
pre-gj0.9 runtime archive + relative `-o`; binary recovered by
refreshing the archive and relinking absolute) -- lessons in
`build-toolchain.md`. A `pkill -f` matched its own shell and died
mid-chain -- lesson in `debugging-method.md`. The Connect-screen back
arrow is by design (Back → Welcome); its broken glyph traced to a stale
foreign tab, not this tree -- no fix. Bonus: `/api/capabilities`
confirms `platform:linux` live (8mk.7 Linux side).

State: stack shut down (ports quiet). gj0.7 closed; `Aurora-1t1`
(fullscreen freeze) is the remaining viz-track item.
